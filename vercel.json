{
  "version": 2,
  "buildCommand": "pnpm --filter client install --frozen-lockfile=false && pnpm --filter client build",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
